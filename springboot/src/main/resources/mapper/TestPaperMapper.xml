<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.TestPaperMapper">

    <select id="selectAll" resultType="com.example.common.vo.TestPaperVO">
        SELECT
            tp.id,
            tp.name as paperName,
            c.name as courseName,
            t.name as teacherName,
            tp.type,
            tp.start as startTime,
            tp.end as endTime,
            tp.time as duration,
            -- 试卷状态：根据当前时间计算
            CASE
                WHEN NOW() &lt; tp.start THEN '未开始'
                WHEN NOW() BETWEEN tp.start AND tp.end THEN '进行中'
                ELSE '已结束'
            END as status
        FROM test_paper tp
        LEFT JOIN course c ON tp.course_id = c.id
        LEFT JOIN teacher t ON tp.teacher_id = t.id
        <where>
            <if test="name != null and name != ''">
                AND tp.name LIKE CONCAT('%', #{name}, '%')
            </if>
        </where>
        ORDER BY tp.id DESC
    </select>

    <!-- insert into test_paper (username, password, ...) values ('test_paper', 'test_paper', ...) -->
    <insert id="insert" parameterType="com.example.entity.TestPaper" useGeneratedKeys="true">
        insert into `test_paper`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="name != null">name,</if>
            <if test="courseId != null">course_id,</if>
            <if test="teacherId != null">teacher_id,</if>
            <if test="type != null">type,</if>
            <if test="start != null">start,</if>
            <if test="end != null">end,</if>
            <if test="time != null">time,</if>
            <if test="questionIds != null">question_ids,</if>

        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="name != null">#{name},</if>
            <if test="courseId != null">#{courseId},</if>
            <if test="teacherId != null">#{teacherId},</if>
            <if test="type != null">#{type},</if>
            <if test="start != null">#{start},</if>
            <if test="end != null">#{end},</if>
            <if test="time != null">#{time},</if>
            <if test="questionIds != null">#{questionIds},</if>

        </trim>
    </insert>

    <delete id="deleteById" parameterType="com.example.entity.TestPaper">
        delete from `test_paper` where id = #{id}
    </delete>

    <update id="updateById" parameterType="com.example.entity.TestPaper">
        update `test_paper`
        <set>
            <if test="name != null">name = #{name},</if>

        </set>
        where id = #{id}
    </update>

</mapper>