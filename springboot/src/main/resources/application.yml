server:
  port: 9090

# 数据库配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/exam_online?useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&useSSL=false&serverTimezone=GMT%2b8&allowPublicKeyRetrieval=true

  # 文件上传大小配置
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 50MB

  # Redis配置
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      timeout: 10000ms
      lettuce:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0

  # 邮件配置（用于发送验证码）
  mail:
    host: smtp.163.com
    port: 465  # 163邮箱使用SSL端口465
    username: tqxyttkx@163.com  # 你的163邮箱
    password: GRiFcRmkzAan7XVM  # 163邮箱的授权码（不是登录密码）
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true  # 开启SSL
          socketFactory:
            class: javax.net.ssl.SSLSocketFactory
            port: 465

  # 配置mybatis实体和xml映射
mybatis:
    mapper-locations: classpath:mapper/*.xml
    configuration:
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
      map-underscore-to-camel-case: true



fileBaseUrl: http://localhost:${server.port}

# Python AI评分服务配置
python:
  service:
    url: http://localhost:5000  # Python服务地址

# AI学习助手配置
ai:
  siliconflow:
    api-key: sk-goeihqvwzmkpwvrbctgjfrjuvczhocqcvovfbfsrddqljocb
    api-url: https://api.siliconflow.cn/v1/chat/completions